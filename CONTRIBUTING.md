# Contributing

感谢你帮助改进 iFans。由于项目会读取 Apple SMC 并控制风扇，请把硬件安全、系统兼容性和低运行开销放在首位。

## 开始之前

- 提交 Issue 前请先搜索现有问题，避免重复报告。
- 涉及风扇控制协议、SMC 读写、守护进程、传感器分类或较大界面调整的改动，请先通过 Issue 讨论方案。
- 问题报告请注明 Mac 型号、Apple Silicon 芯片、macOS 版本、iFans 版本、当前风扇策略和复现步骤。
- 不要上传密码、授权信息、完整系统日志、序列号、设备标识符或未经脱敏的截图。

## 开发流程

1. Fork `Zorabi/iFans`，从最新 `main` 创建范围明确的分支。
2. 保持改动聚焦，并遵循现有 Swift、SwiftUI、AppKit 与守护进程代码结构。
3. 用户可见行为、系统要求、构建方式、传感器策略或安全机制发生变化时，同步更新 `README.md`。
4. 至少执行一次无签名 Release 构建：

   ```bash
   xcodebuild \
     -project iFan.xcodeproj \
     -scheme iFan \
     -configuration Release \
     -derivedDataPath build \
     CODE_SIGNING_ALLOWED=NO \
     build
   ```

5. 打开 Pull Request，说明问题、解决方案、验证结果、测试机型，以及性能或兼容性影响。

## 项目不变量

- GUI 退出、崩溃或停止发送心跳后，守护进程必须可靠恢复系统自动风扇模式。
- 手动策略必须将目标转速限制在硬件报告的最小值和最大值之间；不得移除看门狗或安全恢复路径。
- SMC 键在不同芯片和机型上可能缺失、改变或返回异常数据；读取失败必须安全降级，不能导致崩溃。
- 不要在每次刷新时重新枚举全部 SMC 键。保留元数据缓存、关键传感器快速刷新和其余传感器分批轮询机制。
- 全速模式和完整传感器列表不得引入持续高 CPU 占用；涉及轮询、动画或风扇写入的改动必须进行空闲与全速模式性能检查。
- 菜单栏状态项必须保持固定宽度，温度位数变化不能挤动其他状态栏图标。
- `⌘W` 只隐藏主窗口，不能终止菜单栏监控；从 Dock、启动台或菜单栏重新打开时必须恢复窗口。
- 保留上游项目的 MIT 版权与许可声明，不得删除 `LICENSE-MIT`。

## 验证清单

根据改动范围完成相关检查，并在 Pull Request 中记录结果：

- 系统自动、智能温控、安静、均衡和全速策略均可切换，退出应用后风扇恢复自动模式。
- 智能温控使用最近 10 秒 CPU 热点平均值；普通升档持续 4 秒后生效，达到最高档阈值时立即生效。
- 降档需低于当前阈值 4°C 并持续 20 秒；低于首档或传感器不可用时恢复系统自动模式。
- 自定义曲线可新增、复制、重命名、删除和切换；三套内置曲线分别恢复各自默认档位。
- 旧版单曲线配置升级后不会丢失；默认曲线升级只迁移未修改的旧默认值，不覆盖用户编辑。
- 冷启动与重启后温度为实时读取值，缓存只用于 SMC 元数据。
- CPU 平均温度、热点来源、聚合分类和原始传感器明细正常更新。
- 菜单栏宽度稳定，悬停概要、点击菜单、`⌘W` 和重新打开行为正常。
- 空闲及全速模式下观察 CPU 占用和峰值，确认没有持续性回归。
- Dock 与启动台图标在常用缩放设置下与系统图标视觉尺寸协调。

## 提交规范

每个提交应只处理一个主题，推荐使用以下格式：

```text
{emoji} {type}({scope}): {中文说明}
```

例如：`🐛 fix(smc): 修复热点传感器缺失`、`⚡ perf(sensor): 降低全速模式轮询峰值`、`📝 docs(contributing): 补充贡献指南`。

常用类型包括 `feat`、`fix`、`perf`、`refactor`、`docs`、`test` 和 `chore`。

## 许可证

提交贡献即表示你同意按照项目的 [GNU Affero General Public License v3.0](LICENSE) 授权你的贡献。源自上游 `chansss/iFans` 的代码同时保留 [MIT 许可声明](LICENSE-MIT)。
